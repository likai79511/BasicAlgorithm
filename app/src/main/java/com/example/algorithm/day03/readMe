[栈]：

Stack,一种线性的数据结构，后进先出(LIFO: Last in first out)，相比数组来说操作更少,
因为他的操作只在栈顶，添加和取出只在一端进行，而数组可以指定位置去添加和取出.

| 4 |
| 3 |
| 2 |
| 1 |
|___|

入栈顺序：1,2,3,4  出栈顺序 4,3,2,1
场景：系统调用系统栈，android中 activity的压栈，IDE中的 撤销(undo) 操作等。
递归则是 相同方法且不同参数 多次入栈。

因为这个特性，栈却能用最简单的方式保存状态，且向外（对于使用者）以最简单的形式，而内部可以使用多种数据结构实现。

[队列]
Queue, 线性数据结构，先进先出（队尾添加，队首取出,FIFO: First In First Out），相比数组来说操作更少

| 4 |  <-- 4    队尾
| 3 |
| 2 |
| 1 |  --> 1    队首

入队顺序：1,2,3,4  出队顺序： 1,2,3,4

